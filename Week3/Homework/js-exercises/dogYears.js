"use strict"
function calculateDogAge(age) {
    let dogYears = age * 7;
    return `Your doggie is ${dogYears} years old in dog years!`
};

calculateDogAge(1);
calculateDogAge(3);
calculateDogAge(14);